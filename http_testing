using curl and postman  we can post the and get the data  but we need to run the http server with post and get methods 

curl -X POST http://localhost:8080 -H "Content-Type: application/json" -d '{"sensor_id": "1234", "temperature": 22.5, "humidity": 60}'
